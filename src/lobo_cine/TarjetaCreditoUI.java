/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/GUIForms/JFrame.java to edit this template
 */
package lobo_cine;

import javax.swing.*;
import java.awt.*;

/**
 *
 * @author jessi
 */
public class TarjetaCreditoUI extends javax.swing.JFrame {
    private boolean pagado = false;
    private static final java.util.logging.Logger logger = java.util.logging.Logger.getLogger(TarjetaCreditoUI.class.getName());

    /**
     * Creates new form TarjetaCreditoUI
     */
    public TarjetaCreditoUI() {
        setTitle("Pago con Tarjeta de Crédito");
        setSize(400, 200);
        setLocationRelativeTo(null);
        setDefaultCloseOperation(DISPOSE_ON_CLOSE);

        // 🔥 IMPORTANTE: evita conflicto con NetBeans
        getContentPane().removeAll();
        getContentPane().setLayout(new BorderLayout());

        JLabel label = new JLabel("Ingrese tarjeta (16 dígitos)", JLabel.CENTER);
        JTextField campo = new JTextField();
        JButton btn = new JButton("Pagar");

        add(label, BorderLayout.NORTH);
        add(campo, BorderLayout.CENTER);
        add(btn, BorderLayout.SOUTH);

        btn.addActionListener(e -> {

            String t = campo.getText();

            if (t == null || !t.matches("\\d{16}")) {
                JOptionPane.showMessageDialog(this, "Tarjeta inválida");
                return;
            }

            pagado = true;
            JOptionPane.showMessageDialog(this, "Pago exitoso");
            dispose();
        });

        // 🔥 OBLIGATORIO: mostrar al final
        setVisible(true);
    }

    public boolean isPagado() {
        return pagado;
    }

    /**
     * This method is called from within the constructor to initialize the form.
     * WARNING: Do NOT modify this code. The content of this method is always
     * regenerated by the Form Editor.
     */
    @SuppressWarnings("unchecked")
    // <editor-fold defaultstate="collapsed" desc="Generated Code">//GEN-BEGIN:initComponents
    private void initComponents() {

        setDefaultCloseOperation(javax.swing.WindowConstants.EXIT_ON_CLOSE);

        javax.swing.GroupLayout layout = new javax.swing.GroupLayout(getContentPane());
        getContentPane().setLayout(layout);
        layout.setHorizontalGroup(
            layout.createParallelGroup(javax.swing.GroupLayout.Alignment.LEADING)
            .addGap(0, 400, Short.MAX_VALUE)
        );
        layout.setVerticalGroup(
            layout.createParallelGroup(javax.swing.GroupLayout.Alignment.LEADING)
            .addGap(0, 300, Short.MAX_VALUE)
        );

        pack();
    }// </editor-fold>//GEN-END:initComponents

    /**
     * @param args the command line arguments
     */
    public static void main(String args[]) {
        /* Set the Nimbus look and feel */
        //<editor-fold defaultstate="collapsed" desc=" Look and feel setting code (optional) ">
        /* If Nimbus (introduced in Java SE 6) is not available, stay with the default look and feel.
         * For details see http://download.oracle.com/javase/tutorial/uiswing/lookandfeel/plaf.html 
         */
        try {
            for (javax.swing.UIManager.LookAndFeelInfo info : javax.swing.UIManager.getInstalledLookAndFeels()) {
                if ("Nimbus".equals(info.getName())) {
                    javax.swing.UIManager.setLookAndFeel(info.getClassName());
                    break;
                }
            }
        } catch (ReflectiveOperationException | javax.swing.UnsupportedLookAndFeelException ex) {
            logger.log(java.util.logging.Level.SEVERE, null, ex);
        }
        //</editor-fold>
        new TarjetaCreditoUI();
        /* Create and display the form */
        java.awt.EventQueue.invokeLater(() -> new TarjetaCreditoUI().setVisible(true));
    }

    // Variables declaration - do not modify//GEN-BEGIN:variables
    // End of variables declaration//GEN-END:variables
}
